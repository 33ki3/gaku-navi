# gaku-navi Codex Instructions

## プロジェクト概要

学園アイドルマスター（学マス）のサポートカード情報を閲覧・検索するためのフロントエンド（React + Vite + TypeScript + Tailwind CSS）です。

## Skills

| スキル             | 場所                                 | 概要                                                                     |
| ------------------ | ------------------------------------ | ------------------------------------------------------------------------ |
| data-architecture  | `.agents/skills/data-architecture/`  | データ層アーキテクチャ（types / constant / data・i18n・ディレクトリ構造） |
| coding-conventions | `.agents/skills/coding-conventions/` | コーディング規約（import・コメント・ファイル分割・ベストプラクティス）   |
| ui-components      | `.agents/skills/ui-components/`      | UIコンポーネント構築ガイドライン（スタイル・パターン流用・メンテナンス） |
| git-workflow       | `.agents/skills/git-workflow/`       | Gitブランチ・PR作成・マージ・テンプレート運用                            |

## 詳細仕様

- UI/UXおよびコンポーネント仕様は **ui-components** スキルを参照。

## PR・Issueテンプレート・コミット規約

詳細は **git-workflow** スキルを参照。

- **PR作成時**: `.github/PULL_REQUEST_TEMPLATE.md` に従う。
- **Issue作成時**: `.github/ISSUE_TEMPLATE/` のテンプレートを使用。
- **コミットメッセージ**: `<type>(<scope>): <description>` 形式（`.gitmessage` 参照）。

## 作業フロー

> **【作業範囲】今回の依頼に関連する対応中タスクは、実装と検証が終わるまで完了扱いにしない。無関係な todo の項目には自動で着手しない。**

### todo ファイルの運用ルール

`.github/todo/todo.md` を単一のtodoファイルとして管理する（git管理外）。未完了タスクの整理・再開を依頼された場合や、今回の依頼に関係する場合に参照する。

`archive/` は日付単位（`YYYYMMDD.md`）の履歴ファイルとして運用する。既存ファイルのセクション構成を尊重し、完了済みタスクと解決済みQ&Aはtodoから削除して、対応する日付の archive ファイルに追記する。同日に複数セッションがある場合は同一 archive ファイルに追記する。

todo ファイルは「対応中タスク」「保留中のタスク」「Q&A管理」「追加の指示」の 4 セクションで構成される。

- **追加の指示**: ユーザーが自由に記載する。今回の依頼に関係する場合だけ内容を確認し、継続して管理する必要があるものを「対応中タスク」に `- [ ] ○○: 対応内容の詳細` 形式で移動する。作業を再開したときも、関連する新しい内容が追加されていないか確認する。
- **保留中のタスク**: 今すぐ対応しないが将来対応予定のタスクを記録する。チェックボックス形式（`- [ ] タスク名: 概要`）で管理する。対応開始時に「対応中タスク」に移動する。エージェントは保留タスクを自動的に着手しない。
- **対応中タスク**: チェックボックス形式で管理する。タスク名だけでなく、どういった対応をするのか詳細も併せて記載する。
  - 未対応: `- [ ] タスク名: 対応内容の詳細`
  - 完了済み: `- [x] タスク名: 対応内容の詳細`
  - 対応完了したらすぐに `[x]` に更新する。**作業中は `[x]` を削除しない。**
  - **コード中の `Q:` コメントで回答不要なもの**（リネーム・修正・削除など実装だけで済むもの）は、対応中タスクに入れて対応する。Q&A管理には記載しない。
- **Q&A管理**: コード中の `Q:` コメントのうち、**ユーザーへの回答が必要なもの**（調査結果の報告・設計判断の確認など）のみをここに記載する。コードから `Q:` を削除し、Q と A を記載する。Qを削除した後に回答文やレビュー相手へのメッセージをコードコメントとして残してはならない。ファイル名と行番号をマークダウンリンクで末尾に付記する。形式: `（[src/path/file.ts:行番号](../../src/path/file.ts#L行番号)）`。複数ファイルの場合はカンマ区切りで列挙する。ユーザーが管理するため、エージェントは勝手に削除しない。
  - **チェックボックス形式**: Q: エントリはチェックボックス付きで管理する。エージェントが新規 Q&A を追加する際は `- [ ] Q:` 形式で記載する。ユーザーが確認・解決済みと判断したら `[x]` に更新する。エージェントは自分でチェックを付けない。
  - **フォローアップ Q**: ユーザーが A の下に追加の `Q:` を記載した場合、エージェントはその Q に回答して `A:` を追記する（インデントを揃える）。形式例:
    ```
    - [ ] Q: 質問テキスト
      - A: 回答テキスト（[src/file.ts:行](../../src/file.ts#L行)）
        - Q: フォローアップ質問
          - A: フォローアップ回答
    ```

### todo を使う作業の開始手順

1. 未完了タスクの整理・再開を依頼された場合は `.github/todo/todo.md` を読み込む。
2. 今回の依頼に関連する「追加の指示」があれば、必要なものだけ「対応中タスク」に移動する。

### todo を使った作業の終了手順

1. 完了済みタスク（`[x]`）と解決済み Q&A を `.github/todo/archive/YYYYMMDD.md` に追記する（同日ファイルが既にあれば追記）。

### 作業手順

1. todo を使う作業では、関連する項目と「追加の指示」を確認し、必要なものだけ対応中タスクへ移動する。
2. 今回の依頼に関連する対応中タスクを実装・検証する。コード中の `Q:` コメントは、回答不要（実装のみ）なら対応中タスクとして処理し、回答が必要なら「Q&A管理」に Q と A を記録する（マークダウンリンク `[src/path/file.ts:行番号](../../src/path/file.ts#L行番号)` 形式で末尾に付記）。いずれの場合もコードから `Q:` を削除する。
3. コード内 `Q:` / `TODO:` を検索し、未対応があればタスクに追加。
4. `npx prettier --check "src/**/*.{ts,tsx}"` でフォーマット確認（差分があれば `npx prettier --write` で修正）。未使用コード確認（`npx knip --reporter compact`）、`npm run lint`、`npx tsc -p tsconfig.app.json --noEmit`、`npm run test:run` でエラーがないことを確認する。
5. todo を使った場合は、関連する「追加の指示」「対応中タスク」「Q&A管理」を再チェックする。
6. todo を使った作業では、終了手順に従う。

### 作業完了チェックリスト

タスクを完了とする前に、必ず以下を確認する:

1. todo を使った場合は、今回の依頼に関連する「追加の指示」の内容が反映済みであること。
2. `npx knip --reporter compact` で不要なファイル・エクスポートがないこと。
3. `npx prettier --check "src/**/*.{ts,tsx}"` でフォーマット差分がないこと。`npm run lint`、`npx tsc -p tsconfig.app.json --noEmit`、`npm run test:run` でエラーがないこと。
4. 完了済みタスク（`[x]`）は作業中は残す。
5. Q&A管理セクションの整理はユーザーに確認してから行う。
6. 一時ファイル・デバッグ用ファイルが残っていないこと。

### スクリプト作業ディレクトリ

**coding-conventions** スキルの「一時ファイル・スクリプト」セクションを参照。
